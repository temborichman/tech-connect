specVersion: "v1"
serviceId: "tech-connect"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "tech-connect-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
